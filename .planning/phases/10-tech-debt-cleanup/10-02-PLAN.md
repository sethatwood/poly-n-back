---
phase: 10-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - DEPS-08

must_haves:
  truths:
    - "Android project has fresh web assets synced via `npx cap sync android`"
    - "Android build compiles and runs in Android Studio without errors"
  artifacts: []
  key_links:
    - from: "android/app/src/main/assets/"
      to: "dist/"
      via: "npx cap sync android copies web build into native project"
      pattern: "capacitor.config"
---

<objective>
Sync web assets to the Android native project and verify the Android build compiles and runs in Android Studio.

Purpose: Close the Android build verification gap that has been open since Phase 3 (Capacitor migration). Every prior phase deferred Android verification due to environment constraints.
Output: Verified Android build, any issues documented.
</objective>

<execution_context>
@/Users/yayseth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yayseth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tech-debt-cleanup/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build web assets and sync to Android project</name>
  <files>android/app/src/main/assets/</files>
  <action>
Run the following commands sequentially:
1. `npm run build` -- produce fresh web assets in `dist/`
2. `npx cap sync android` -- sync web assets + Capacitor config to the Android native project

This ensures the Android project has the latest code before the human verification step.
  </action>
  <verify>
    <automated>ls android/app/src/main/assets/public/index.html && echo "Assets synced"</automated>
  </verify>
  <done>Web assets are built and synced to the Android native project. `android/app/src/main/assets/public/index.html` exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Android native project synced with latest web assets and Capacitor 8 configuration (AGP 8.13.0, compileSdk 36, targetSdk 36, minSdk 24).</what-built>
  <how-to-verify>
    1. Open Android Studio
    2. Open the `android/` directory as an existing project
    3. Wait for Gradle sync to complete (Android Studio may prompt to install SDK 36 or update tools -- accept these)
    4. Select an emulator or connected device
    5. Click Run (green play button)
    6. Verify the app launches and shows the Poly N-Back menu screen
    7. Start a game and verify basic gameplay works (stimulus appears, buttons respond, audio plays if available)
    8. If the build fails, note the exact error message
  </how-to-verify>
  <resume-signal>Type "approved" if the Android build runs successfully, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npx cap sync android` succeeds without errors
3. Android Studio Gradle sync completes
4. App builds and runs on Android emulator or device
5. Basic gameplay functions (menu, game start, responses, game over)
</verification>

<success_criteria>
- Android build compiles in Android Studio without errors
- App launches on emulator or device and shows the menu screen
- Basic game flow works (start game, play round, see game over)
</success_criteria>

<output>
After completion, create `.planning/phases/10-tech-debt-cleanup/10-02-SUMMARY.md`
</output>
