---
phase: 10-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/audioStore.ts
  - src/main.ts
  - src/stores/__tests__/audioStore.test.ts
  - src/stores/__tests__/persistenceStore.test.ts
  - src/stores/__tests__/gameFlow.integration.test.ts
  - src/stores/__tests__/stateTransitions.integration.test.ts
  - eslint.config.js
  - .github/workflows/ci.yml
  - package.json
autonomous: true
requirements:
  - DEPS-08

must_haves:
  truths:
    - "`npm run lint` exits with zero errors on all source and test files"
    - "CI `check` job includes a lint step that fails the build on lint errors"
    - "`vue/block-lang` ESLint rule is active and enforces `lang=\"ts\"` on all `.vue` components"
    - "Every `eslint-disable-next-line` comment includes a `--` justification suffix explaining why the cast is necessary"
    - "No unaddressed `any` casts exist -- each is either properly typed or has an explicit eslint-disable with justification"
  artifacts:
    - path: "eslint.config.js"
      provides: "ESLint config with vue/block-lang re-enabled (line removed)"
      contains: "defineConfigWithVueTs"
    - path: ".github/workflows/ci.yml"
      provides: "CI check job with lint step before type-check"
      contains: "npm run lint:check"
    - path: "package.json"
      provides: "lint:check script for CI (eslint without --fix)"
      contains: "lint:check"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "npm run lint:check script"
      pattern: "lint:check"
    - from: "eslint.config.js"
      to: "*.vue files"
      via: "vue/block-lang rule enforces lang=\"ts\" on all script blocks"
      pattern: "block-lang"
---

<objective>
Fix all 20 ESLint errors across 5 files, re-enable the `vue/block-lang` rule, add a `lint:check` npm script, and integrate lint into the CI pipeline.

Purpose: Close the LINT-CI-GAP identified in the v1.0 milestone audit so that lint regressions are caught on every push and M2 builds on a clean, enforced linting foundation.
Output: Zero-error `npm run lint`, CI lint step, re-enabled `vue/block-lang` rule, all `eslint-disable` comments justified.
</objective>

<execution_context>
@/Users/yayseth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yayseth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tech-debt-cleanup/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add eslint-disable-next-line justifications to all 20 lint errors</name>
  <files>
    src/stores/audioStore.ts,
    src/main.ts,
    src/stores/__tests__/audioStore.test.ts,
    src/stores/__tests__/persistenceStore.test.ts,
    src/stores/__tests__/gameFlow.integration.test.ts,
    src/stores/__tests__/stateTransitions.integration.test.ts
  </files>
  <action>
Add `// eslint-disable-next-line @typescript-eslint/no-explicit-any -- {justification}` on the line immediately before each `any` cast. Each comment MUST include the `--` separator followed by a reason. Also fix the one `@typescript-eslint/no-unused-vars` error.

**src/stores/audioStore.ts** (1 error):
- Line 31: `(window as any).webkitAudioContext` -- Add disable comment on line 30 with justification: `Safari compat: webkitAudioContext is not in the standard Window type definition`

**src/main.ts** (0 new errors, 1 existing disable needs justification):
- Line 33: The existing `// eslint-disable-next-line @typescript-eslint/no-explicit-any` has no justification. Replace it with: `// eslint-disable-next-line @typescript-eslint/no-explicit-any -- dev-only: expose store on window for console debugging`

**src/stores/__tests__/audioStore.test.ts** (12 errors -- 4 identical blocks at lines 92-94, 154-156, 171-173, 271-273):
- Each block has 3 consecutive lines: `(globalThis as any).AudioContext = undefined;`, `(window as any).AudioContext = undefined;`, `(window as any).webkitAudioContext = undefined;`
- Add disable comment before EACH of the 3 lines in EACH of the 4 blocks (12 comments total)
- Justification for all 12: `test mock: remove browser API to simulate unsupported environment`

**src/stores/__tests__/persistenceStore.test.ts** (3 errors):
- Line 11: `(Preferences as any)._reset()` -- Justification: `test mock: _reset is a test-only method on the Preferences mock`
- Line 86: `(Preferences as any).set = vi.fn()...` -- Justification: `test mock: override set to simulate disk full error`
- Line 91: `(Preferences as any).set = originalSet;` -- Justification: `test mock: restore original set method after test`

**src/stores/__tests__/gameFlow.integration.test.ts** (1 error):
- Line 18: `(Preferences as any)._reset()` -- Justification: `test mock: _reset is a test-only method on the Preferences mock`

**src/stores/__tests__/stateTransitions.integration.test.ts** (3 errors):
- Line 54: `let app: App;` is declared but `app` is never read. Fix by removing the `app` variable entirely. The `withSetup()` call at line 59 returns `{ result, app }` -- destructure without `app`: change `const setup = withSetup(...)` pattern to NOT assign `setup.app` to `app`. Specifically: if the code is `app = setup.app; lifecycle = setup.result;` change to `lifecycle = withSetup(gameStore).result;` or simply omit the `app` assignment. Check the actual code pattern first.
- Line 76: `(Preferences as any)._reset()` -- Justification: `test mock: _reset is a test-only method on the Preferences mock`
- Line 226: `(Preferences as any)._reset()` -- Justification: `test mock: _reset is a test-only method on the Preferences mock`

IMPORTANT: Line numbers from the research are a starting reference. Read each file first to find exact line numbers before editing. The `eslint-disable-next-line` comment MUST be placed on the line IMMEDIATELY preceding the line with the `any` cast.
  </action>
  <verify>
    <automated>npx eslint src/stores/audioStore.ts src/main.ts src/stores/__tests__/audioStore.test.ts src/stores/__tests__/persistenceStore.test.ts src/stores/__tests__/gameFlow.integration.test.ts src/stores/__tests__/stateTransitions.integration.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>All 20 ESLint errors resolved. Every `eslint-disable-next-line` comment includes a `--` justification suffix. The unused `app` variable is removed without breaking the test.</done>
</task>

<task type="auto">
  <name>Task 2: Re-enable vue/block-lang, add lint:check script, integrate lint into CI</name>
  <files>
    eslint.config.js,
    package.json,
    .github/workflows/ci.yml
  </files>
  <action>
Three targeted changes:

**1. eslint.config.js** -- Remove the `vue/block-lang` override:
- Delete the line `'vue/block-lang': 'off',` and its comment `// Temporarily off during incremental TS migration...`
- The `@vue/eslint-config-typescript` `vueTsConfigs.recommended` already configures this rule as `["error", { script: { lang: ["ts"], allowNoLang: false } }]`, so removing the override re-enables enforcement.

**2. package.json** -- Add a `lint:check` script for CI use:
- Add to `"scripts"`: `"lint:check": "eslint ."`
- This runs eslint WITHOUT `--fix`, so auto-fixable errors fail CI instead of being silently fixed.
- Keep the existing `"lint": "eslint . --fix"` script for local developer convenience.

**3. .github/workflows/ci.yml** -- Add lint step to the `check` job:
- Add `- run: npm run lint:check` as a new step BEFORE the `npm run type-check` step.
- This gives the fastest feedback: lint errors are reported before the slower type-check and test steps.

The final `check` job step order should be:
1. `npm ci`
2. `npm run lint:check`  (NEW)
3. `npm run type-check`
4. `npm run test:unit`
5. `npm run build`
  </action>
  <verify>
    <automated>npx eslint . 2>&1 | tail -3 && npm run lint:check 2>&1 | tail -3</automated>
  </verify>
  <done>`npx eslint .` passes with zero errors. `npm run lint:check` passes with zero errors. `vue/block-lang` is active (re-enabling enforces `lang="ts"` on all .vue files). CI workflow includes lint:check step before type-check.</done>
</task>

</tasks>

<verification>
Run the full verification suite to confirm no regressions:
1. `npx eslint .` -- zero errors, zero warnings
2. `npm run lint:check` -- exits 0
3. `npm run lint` -- exits 0 (with --fix, same result since nothing to fix)
4. `npm run type-check` -- vue-tsc passes (no regressions from eslint-disable comments)
5. `npm run test:unit` -- all unit tests pass (no regressions from unused var fix)
6. `npm run build` -- vite build succeeds
</verification>

<success_criteria>
- `npm run lint` exits 0 with zero errors on all source and test files
- `npm run lint:check` (new script, no --fix) exits 0
- CI `check` job in `.github/workflows/ci.yml` includes `npm run lint:check` before `npm run type-check`
- `vue/block-lang` rule is enforced (the `'off'` override is removed from eslint.config.js)
- Every `eslint-disable-next-line` in the codebase has a `--` justification comment
- `npm run test:unit` passes (no regressions from stateTransitions unused var fix)
</success_criteria>

<output>
After completion, create `.planning/phases/10-tech-debt-cleanup/10-01-SUMMARY.md`
</output>
