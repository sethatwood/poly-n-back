---
phase: 06-component-extraction
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/GameScreen.vue
  - src/App.vue
autonomous: false
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "GameScreen component composes GameTimer, Stimulus, ResponseButtons, ScoreDisplay, GameOverDisplay, ConfigStart (restart), audio toggle, and Footer"
    - "App.vue is reduced to ~80 lines of logic (thin shell with screen routing via Transition and overlay mounting)"
    - "Screen-fade transition animates correctly between MenuScreen and GameScreen with key attributes"
    - "All animations (score pulse, strike shake, feedback flash, timer urgency pulse) work identically to before extraction"
    - "Game flow (menu to game to pause to resume to game over to menu) works without state leaks or broken transitions"
    - "High score line is visible during both active play and game-over states"
    - "Restart ConfigStart in GameScreen correctly shows when game is stopped or strikes >= 3"
  artifacts:
    - path: "src/components/GameScreen.vue"
      provides: "Game screen composing all sub-components with props/emits"
      contains: "GameScreen"
    - path: "src/App.vue"
      provides: "Thin shell with screen routing and overlay mounting"
      min_lines: 60
  key_links:
    - from: "src/App.vue"
      to: "src/components/MenuScreen.vue"
      via: "v-if=\"showModal\" with key=\"menu\""
      pattern: "MenuScreen.*v-if.*showModal"
    - from: "src/App.vue"
      to: "src/components/GameScreen.vue"
      via: "v-else with key=\"game\""
      pattern: "GameScreen.*v-else"
    - from: "src/components/GameScreen.vue"
      to: "src/components/GameTimer.vue"
      via: "child component with props"
      pattern: "GameTimer"
    - from: "src/components/GameScreen.vue"
      to: "src/components/ResponseButtons.vue"
      via: "child component with props and respond emit"
      pattern: "ResponseButtons.*@respond"
    - from: "src/components/GameScreen.vue"
      to: "src/components/ScoreDisplay.vue"
      via: "child component with v-if=\"!gameStore.isStopped\""
      pattern: "ScoreDisplay.*v-if"
    - from: "src/components/GameScreen.vue"
      to: "src/components/GameOverDisplay.vue"
      via: "child component with v-else"
      pattern: "GameOverDisplay.*v-else"
---

<objective>
Create GameScreen component and reduce App.vue to a thin shell with screen routing and overlay mounting.

Purpose: Completes the component extraction by wiring Plan 01's sub-components into GameScreen and replacing App.vue's inline template sections with screen component references. After this plan, App.vue is a ~80-line thin shell and all game UI lives in focused, composable components.

Output: `src/components/GameScreen.vue` (new) and `src/App.vue` (reduced to thin shell). All gameplay behavior identical to before extraction.
</objective>

<execution_context>
@/Users/yayseth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yayseth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-component-extraction/06-RESEARCH.md
@.planning/phases/06-component-extraction/06-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 components. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/components/GameTimer.vue (Plan 01):
```javascript
name: 'GameTimer',
props: {
  timeLeft: { type: Number, required: true },
  isPaused: { type: Boolean, required: true },
  showFeedbackToast: { type: Boolean, required: true },
  feedbackType: { type: String, default: null },
}
// No emits
```

From src/components/ResponseButtons.vue (Plan 01):
```javascript
name: 'ResponseButtons',
props: {
  buttons: { type: Array, required: true },
  respondedThisTurn: { type: Object, required: true },
  isEarlyInGame: { type: Boolean, required: true },
  isPaused: { type: Boolean, required: true },
  feedbackClass: { type: Function, required: true },
}
emits: ['respond']
```

From src/components/ScoreDisplay.vue (Plan 01):
```javascript
name: 'ScoreDisplay',
props: {
  score: { type: Number, required: true },
  incorrectResponses: { type: Number, required: true },
  scoreAnimating: { type: Boolean, required: true },
  strikeAnimating: { type: Boolean, required: true },
}
// No emits
```

From src/components/GameOverDisplay.vue (Plan 01):
```javascript
name: 'GameOverDisplay',
props: {
  score: { type: Number, required: true },
  previousPotentialCorrectAnswers: { type: Number, required: true },
  finalScoreAccuracy: { type: Number, required: true },
  highScoreData: { type: Object, required: true },
  highScoreAccuracy: { type: Number, required: true },
}
emits: ['reset-high-score']
```

From src/components/MenuScreen.vue (Plan 01):
```javascript
name: 'MenuScreen',
props: {
  nBackInput: { type: Number, required: true },
  timeLeftInput: { type: Number, required: true },
  nBack: { type: Number, required: true },
}
emits: ['update:nBackInput', 'update:timeLeftInput', 'start-game', 'show-tutorial']
```

From src/App.vue (current script setup - to be preserved):
```javascript
const gameStore = useGameStore();
const persistenceStore = usePersistenceStore();
const { scoreAnimating, strikeAnimating } = useAnimations(gameStore);
const { showFeedbackToast, feedbackClass } = useFeedback(gameStore);
const { showModal, startGame: startGameLifecycle, handlePause, handleResume, handleQuit, handleGameOverClose, handlePlayAgain: playAgainLifecycle, handleMainMenu } = useGameLifecycle(gameStore);
const nBackInput = ref(gameStore.nBack);
const timeLeftInput = ref(gameStore.timeLeft);
const showTutorial = ref(false);
// ... onMounted, watchers, wrappers all stay in App.vue
```

From src/App.vue (current responseButtons constant - moves to ResponseButtons.vue in Plan 01):
```javascript
const responseButtons = [
  { type: 'color', label: 'Color' },
  { type: 'emoji', label: 'Emoji' },
  { type: 'position', label: 'Position' },
  { type: 'shape', label: 'Shape' },
];
```

Existing components that STAY as direct imports in App.vue (overlays):
```javascript
import GameOverModal from './GameOverModal.vue';
import PauseModal from './PauseModal.vue';
import TutorialOverlay from './TutorialOverlay.vue';
import GameHint from './GameHint.vue';
import AchievementToast from './AchievementToast.vue';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GameScreen component composing all sub-components</name>
  <files>src/components/GameScreen.vue</files>
  <action>
Create GameScreen.vue that composes the 4 sub-components (GameTimer, ResponseButtons, ScoreDisplay, GameOverDisplay) plus existing Stimulus, ConfigStart, and Footer.

**Props interface:**
```javascript
props: {
  gameStore: { type: Object, required: true },
  nBackInput: { type: Number, required: true },
  timeLeftInput: { type: Number, required: true },
  scoreAnimating: { type: Boolean, required: true },
  strikeAnimating: { type: Boolean, required: true },
  showFeedbackToast: { type: Boolean, required: true },
  feedbackClass: { type: Function, required: true },
}
```

**Emits:**
```javascript
emits: ['respond', 'update:nBackInput', 'update:timeLeftInput', 'start-game', 'toggle-audio', 'reset-high-score']
```

**Template structure** (replaces App.vue lines 28-178):
```html
<div class="w-screen max-w-md mx-auto px-4 text-center uppercase text-white relative">
  <GameTimer
    :time-left="gameStore.timeLeft"
    :is-paused="gameStore.isPaused"
    :show-feedback-toast="showFeedbackToast"
    :feedback-type="gameStore.lastFeedback.type"
  />
  <Stimulus
    class="mb-3"
    :color="gameStore.currentStimulus.color"
    :emoji="gameStore.currentStimulus.emoji"
    :position="gameStore.currentStimulus.position"
    :shape="gameStore.currentStimulus.shape"
    :flash-border="gameStore.flashBorder"
  />
  <ResponseButtons
    :buttons="responseButtons"
    :responded-this-turn="gameStore.respondedThisTurn"
    :is-early-in-game="gameStore.isEarlyInGame"
    :is-paused="gameStore.isPaused"
    :feedback-class="feedbackClass"
    @respond="$emit('respond', $event)"
  />
  <div class="text-center">
    <ScoreDisplay
      v-if="!gameStore.isStopped"
      :score="gameStore.score"
      :incorrect-responses="gameStore.incorrectResponses"
      :score-animating="scoreAnimating"
      :strike-animating="strikeAnimating"
    />
    <GameOverDisplay
      v-else
      :score="gameStore.score"
      :previous-potential-correct-answers="gameStore.previousPotentialCorrectAnswers"
      :final-score-accuracy="gameStore.finalScoreAccuracy"
      :high-score-data="gameStore.highScoreData"
      :high-score-accuracy="gameStore.highScoreAccuracy"
      @reset-high-score="$emit('reset-high-score')"
    />
    <!-- High score line visible during ACTIVE play (game-over has its own in GameOverDisplay) -->
    <p v-if="!gameStore.isStopped" class="mt-2 text-sm uppercase text-gray-500">
      High Score: {{ gameStore.highScoreData.score }}/{{ gameStore.highScoreData.potentialCorrectAnswers }}
      ({{ gameStore.highScoreAccuracy }}%)
      <span v-if="gameStore.highScoreData.nBack">N={{ gameStore.highScoreData.nBack }}</span>
      <span class="p-1 cursor-pointer" @click="$emit('reset-high-score')">&#x24E7;</span>
    </p>
  </div>
  <div v-if="gameStore.isStopped || gameStore.incorrectResponses >= 3">
    <ConfigStart
      :n-back="nBackInput"
      :time-left="timeLeftInput"
      @update:n-back="$emit('update:nBackInput', $event)"
      @update:time-left="$emit('update:timeLeftInput', $event)"
      @start-game="$emit('start-game')"
    />
  </div>
  <div class="mt-3">
    <button
      class="text-xs text-gray-600 bg-gray-600 hover:bg-gray-500 p-1 rounded-full focus:outline-hidden"
      @click="$emit('toggle-audio')"
    >
      <img
        v-if="gameStore.isAudioEnabled"
        class="h-5 w-5"
        :src="volumeUpIcon"
        alt="Volume Up"
      />
      <img
        v-else
        class="h-5 w-5"
        :src="volumeMuteIcon"
        alt="Volume Mute"
      />
    </button>
  </div>
  <Footer />
</div>
```

**Script section:**
Import sub-components from `./` (same directory), import Stimulus, ConfigStart, Footer from `../` (parent). Import volume icons from `../assets/`. Define the `responseButtons` constant inside setup (moved from App.vue). Return all props, the responseButtons constant, and the icon imports.

**No scoped CSS needed** -- all animation CSS lives in the sub-components. GameScreen is purely a composition layer.

Use `setup()` function pattern with `name: 'GameScreen'`.

**Critical: High score dual-visibility.** The high score line currently sits outside the v-if/v-else toggle in App.vue (line 136-147), visible in both active-play and game-over states. After extraction:
- GameOverDisplay includes the high score as part of game-over results
- GameScreen renders a standalone high score `<p>` during active play (`v-if="!gameStore.isStopped"`)
This preserves the existing behavior where high score is always visible.
  </action>
  <verify>
    <automated>cd /Users/yayseth/Projects/poly-n-back && test -f src/components/GameScreen.vue && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>GameScreen.vue exists, composing GameTimer, Stimulus, ResponseButtons, ScoreDisplay, GameOverDisplay, ConfigStart (restart), audio toggle, and Footer. High score visible in both states. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce App.vue to thin shell with screen routing and overlays</name>
  <files>src/App.vue</files>
  <action>
Replace App.vue's template and script to use MenuScreen and GameScreen components instead of inline markup.

**Template (~35 lines):**
```html
<template>
  <div class="min-h-screen flex items-center justify-center overflow-hidden game-background">
    <Transition name="screen-fade" mode="out-in">
      <MenuScreen
        v-if="showModal"
        key="menu"
        :n-back-input="Number(nBackInput)"
        :time-left-input="Number(timeLeftInput)"
        :n-back="gameStore.nBack"
        @update:n-back-input="nBackInput = $event"
        @update:time-left-input="timeLeftInput = $event"
        @start-game="startGame"
        @show-tutorial="showTutorial = true"
      />
      <GameScreen
        v-else
        key="game"
        :game-store="gameStore"
        :n-back-input="Number(nBackInput)"
        :time-left-input="Number(timeLeftInput)"
        :score-animating="scoreAnimating"
        :strike-animating="strikeAnimating"
        :show-feedback-toast="showFeedbackToast"
        :feedback-class="feedbackClass"
        @respond="respond"
        @update:n-back-input="nBackInput = $event"
        @update:time-left-input="timeLeftInput = $event"
        @start-game="startGame"
        @toggle-audio="toggleAudio"
        @reset-high-score="resetHighScore"
      />
    </Transition>

    <!-- Pause Button (outside transition, viewport top right) -->
    <button
      v-if="!showModal && !gameStore.isStopped"
      class="fixed right-4 p-2 text-gray-500 hover:text-white transition-colors z-30"
      style="top: calc(env(safe-area-inset-top, 0px) + 1rem)"
      title="Pause"
      @click="handlePause"
    >
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 9v6m4-6v6m7-3a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
    </button>

    <!-- Overlays -->
    <AchievementToast />
    <GameHint />
    <PauseModal :show="gameStore.isPaused" :score="gameStore.score" :strikes="gameStore.incorrectResponses" @resume="handleResume" @quit="handleQuit" />
    <GameOverModal :show="gameStore.showGameOverModal" :score="gameStore.score" :possible-points="gameStore.previousPotentialCorrectAnswers" :accuracy="gameStore.finalScoreAccuracy" :n-back="gameStore.nBack" :timer="Number(timeLeftInput)" :is-new-high-score="gameStore.isNewHighScore" @close="handleGameOverClose" @play-again="handlePlayAgain" @main-menu="handleMainMenu" />
    <TutorialOverlay :show="showTutorial" @complete="handleTutorialComplete" />
  </div>
</template>
```

**Script changes:**
- Remove imports for: IntroHead, IntroContent, ConfigStart, Stimulus, Footer, volumeUpIcon, volumeMuteIcon
- Add imports for: MenuScreen from `./components/MenuScreen.vue`, GameScreen from `./components/GameScreen.vue`
- Update components object: replace IntroHead, IntroContent, ConfigStart, Stimulus, Footer with MenuScreen, GameScreen
- Remove from setup return: `buttonClass`, `responseButtons`, `volumeUpIcon`, `volumeMuteIcon` (these are now in sub-components/GameScreen)
- Keep ALL composable calls, input state, watchers, lifecycle handlers unchanged in setup
- The setup function body stays the same except for the 4 removed return values

**Style section (~16 lines):**
Remove ALL animation CSS that was migrated to child components:
- Remove: countdown-text, score-pulse, animate-score-pulse, strike-shake, animate-strike-shake, pulse-urgent, animate-pulse-urgent, correct-flash, animate-correct-flash, incorrect-flash, animate-incorrect-flash, feedback-subtle transitions
- Keep ONLY: screen-fade transitions (screen-fade-enter-active, screen-fade-leave-active, screen-fade-enter-from, screen-fade-leave-to)

**Critical checks after editing:**
1. The `<Transition name="screen-fade" mode="out-in">` wraps MenuScreen (key="menu") and GameScreen (key="game") as direct children -- no extra wrapper divs
2. Pause button stays in App.vue template (fixed position overlay)
3. All 5 overlay components (AchievementToast, GameHint, PauseModal, GameOverModal, TutorialOverlay) stay in App.vue
4. All composable wiring in setup() is unchanged
5. `npx eslint src/App.vue src/components/` passes
  </action>
  <verify>
    <automated>cd /Users/yayseth/Projects/poly-n-back && npx vite build 2>&1 | tail -5 && wc -l src/App.vue</automated>
  </verify>
  <done>App.vue is reduced to approximately 80 lines of logic (template ~50 lines, script ~55 lines, style ~16 lines). Build passes. All previous gameplay behavior preserved: menu/game transition, all animations, all overlays. GameScreen composes sub-components correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete component extraction visually and functionally</name>
  <files>src/App.vue, src/components/GameScreen.vue, src/components/MenuScreen.vue</files>
  <action>
Human verification checkpoint. No automated action -- user visually and functionally verifies the extraction.

What was built: App.vue decomposed into MenuScreen, GameScreen, and 4 game sub-components (GameTimer, ResponseButtons, ScoreDisplay, GameOverDisplay). All animation CSS migrated to child components.

How to verify:
1. Run `npm run dev` and open the app in browser
2. Menu screen: Verify IntroHead, ConfigStart (n-back/timer inputs), IntroContent, and Footer render correctly
3. Start a game: Click "Start Game" -- verify screen-fade transition animates smoothly
4. Timer: Verify countdown number displays, and when time reaches 2s or below the amber urgency pulse animation fires
5. Response buttons: Click buttons -- verify feedback flash (green border for correct, red for incorrect) and disabled state
6. Score display: Verify score pulse animation on correct answer, strike shake animation on incorrect
7. Feedback indicator: Verify checkmark/X indicator appears briefly below timer after each response
8. Game over: Let strikes reach 3 -- verify inline game-over display (final score, accuracy, high score) appears
9. High score: Verify high score line is visible both during active play AND after game over
10. Restart: Verify ConfigStart appears after game over and "Start Game" works
11. Pause flow: Start a game, click pause button (top right) -- verify PauseModal appears. Resume and quit both work.
12. Audio toggle: Verify the audio toggle button works
13. Tutorial: Clear app data, reload -- verify tutorial overlay appears
  </action>
  <verify>
    <automated>cd /Users/yayseth/Projects/poly-n-back && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms all animations, transitions, game flows, and overlays work identically to before extraction. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
1. `src/components/GameScreen.vue` exists and composes all 4 sub-components + Stimulus + ConfigStart + Footer
2. `src/App.vue` line count is approximately 120 total (template ~50, script ~55, style ~16), with logic portion at ~80 lines
3. `npx vite build` passes with zero errors
4. `npx eslint src/App.vue src/components/` passes with zero errors
5. Screen-fade transition works between MenuScreen and GameScreen
6. All 4 animation types work: score pulse, strike shake, button feedback flash, timer urgency pulse
7. Game flow (menu -> game -> pause -> resume -> game over -> menu) completes without errors
8. No duplicate store references -- only App.vue calls useGameStore()
</verification>

<success_criteria>
- App.vue is reduced to approximately 80 lines of logic (thin shell with screen routing and overlay mounting)
- GameScreen exists and composes GameTimer, ResponseButtons, ScoreDisplay, GameOverDisplay, Stimulus, ConfigStart, Footer
- All animations work identically to before extraction
- All game flows work without state leaks or broken transitions
- Human verification confirms visual and functional correctness
</success_criteria>

<output>
After completion, create `.planning/phases/06-component-extraction/06-02-SUMMARY.md`
</output>
